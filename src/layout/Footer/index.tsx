import React from 'react';
import style from './index.module.scss';

interface Props {}

const Footer = (props: Props) => {
  return <div className={style.title}>Footer</div>;
};

export default Footer;
